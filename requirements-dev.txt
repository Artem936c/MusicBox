# Зависимости для разработки и тестов.
# Установка: pip install -r requirements-dev.txt

-r requirements.txt

# Тесты (asyncio_mode = auto задан в pytest.ini)
pytest>=8.0
pytest-asyncio>=0.23
anyio>=4.2

# Линтер и форматтер
ruff>=0.4
